import styled from 'styled-components';

export default styled.h3`
  margin: 0;
  padding: 0.75rem 0;
  line-height: 1.2;
  font-size: 1.25rem;
  font-weight: 300;
  font-family: ${(props) => props.theme.font.mono};
`;
